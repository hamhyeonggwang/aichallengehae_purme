# 보강 브릿지 — Project Instructions (L2)

> 결석으로 빈 치료 회기를 대기 중인 재원 아동에게 연결하는 도구
> AI 챌린지 해 예선·본선 산출물 · Apache License 2.0 공개 예정

---

## Pointer to L1

이 프로젝트는 Occupation Flow AI OS L1의 Constraint(C1~C4)·Default(D1~D2)를 상속한다.
L1 규칙 텍스트를 이 문서에 복제하지 않는다. 아래에는 **조임(tightening)과 프로젝트 고유 Procedure만** 기재한다.

---

## Constraint 조임

L1 대비 이 프로젝트에서 더 엄격하게 적용한다.

- **C1 조임** — 회수율·기준선·정확도 수치는 **측정 전까지 코드·문서·커밋 메시지 어디에도 쓰지 않는다.** 예시 데이터의 수치는 가상임을 파일 상단에 명시한다.
- **C2 조임** — 실제 아동·치료사·진료 정보는 저장소에 **어떤 형태로도 들어가지 않는다.** 실명처럼 보이는 더미 데이터도 만들지 않는다. 아동은 `아동 가~자`, 치료사는 `PT-01`·`OT-01` 형식만 사용한다.
- **C4 조임** — 범위 확장 요청이 오면 구현 전에 `docs/SPEC-SCOPE.md`의 Out 목록과 대조한다. 목록에 있으면 구현하지 않고 이유를 밝힌다.

---

## 핵심 원칙 — 이 프로젝트의 주장

**AI에게 배정 판단을 맡기지 않는다.**

| 구간 | 담당 | 근거 |
|---|---|---|
| 결석 문자 해석 | **AI** | 자유 형식·구어체. 규칙 불가 |
| 빈 회기 계산 | 규칙 | 결정론 필요 |
| 후보 매칭·순위 | **규칙** | 재현·검증 가능해야 함 |
| 최종 배정 확정 | **사람** | 책임 소재 |
| 안내문 생성 | AI/템플릿 | |

이 분리는 제품의 주장 자체다. **규칙 엔진에 AI 호출을 삽입하지 않는다.**

**우선순위는 형평 기준이다.**
`잔여 기회 적은 순 → 대기 경과일 긴 순 → ID 오름차순`
수익성이나 배정 용이성으로 정렬하지 않는다. 이 한 줄이 영리 도구와의 차이를 코드로 드러내는 지점이므로 주석으로 근거를 남긴다.

---

## Procedure (이 프로젝트의 정본)

### 작업 시작 전

1. `docs/SPEC-SCOPE.md`의 Out 목록 확인 — 요청이 범위 밖이면 구현 전에 알린다
2. `docs/SPEC-RULES.md` 확인 — 매칭 규칙은 이 문서가 정본이며 임의 변경 금지
3. 개인정보 관련 작업이면 `docs/SPEC-PRIVACY.md` 확인

### 구현 규칙

- 주석은 한국어. 규칙 엔진의 정렬 조건에는 반드시 근거를 명시한다
- 컴포넌트를 과분할하지 않는다. 단일 페이지 프로토타입이다
- 상태관리 라이브러리를 도입하지 않는다. `useState`로 충분하다
- `localStorage` / `sessionStorage`를 사용하지 않는다
- Gemini API 키를 클라이언트에서 호출하지 않는다. 서버 라우트 경유만 허용
- 매칭 규칙 변경 시 3단계 tie-breaker를 유지한다 (재현성 보장)

### 완료 판정

각 기능은 `docs/PRD.md`의 수용 기준을 통과해야 완료로 간주한다.
특히 다음 항목은 생략 불가:

- [ ] API 키 없이도 전 기능이 동작한다 (폴백 전환)
- [ ] 부분 결석 예시에서 규칙 파서는 실패하고 AI는 성공한다
- [ ] 배포 후 시크릿 창에서 로그인 없이 접속된다
- [ ] 익명화 단계가 서버 호출 **전에** 실행되어, 원문이 API로 전송되지 않는다
- [ ] 실행 경로 패널의 단계가 `n8n/workflow.json`의 노드 구성과 일치한다

---

## 스택

L1 Stack Reference를 상속하되 이 프로젝트에서 다음을 명시적으로 무효화한다(D1 상향 우선).

| 레이어 | L1 기본값 | 이 프로젝트 | 근거 |
|---|---|---|---|
| DB | Supabase | **없음** | 상태 저장 불필요. 개발 시간만 소모 |
| Auth | Supabase Auth | **없음** | 심사위원 즉시 접속이 필수 요건 |
| Frontend | Next.js 14 + Tailwind | 동일 | |
| Deploy | Vercel | 동일 | |
| Automation | n8n | 동일 | 본선 실행 경로 |

디자인 토큰은 L1 참조(frontend-design 스킬). 이 프로젝트 고유값은 없다.

---

## 파일 구조

```
CLAUDE.md                 # 이 문서 — 항상 로드됨
docs/
  PRD.md                  # 제품 사양 · 수용 기준
  SPEC-SCOPE.md           # In/Out 경계 ← 범위 판단의 정본
  SPEC-RULES.md           # 규칙 엔진 명세 ← 매칭 로직 정본
  SPEC-PRIVACY.md         # 익명화·재식별 파이프라인
  DECISIONS.md            # 결정 기록 + Assumption Ledger
app/
  page.tsx                # 단일 페이지. 컴포넌트 인라인 허용
  layout.tsx · globals.css
  api/extract/route.ts    # Claude 프록시 + 폴백
components/
  PipelineTrace.tsx       # 실행 경로 시각화 (n8n 노드와 1:1)
lib/
  mockData.ts             # 8종 치료 · 치료사 80 · 대기 명단
  matcher.ts              # 규칙 엔진 (SPEC-RULES.md 구현)
  fallbackParser.ts       # 정규식 파서
  anonymize.ts            # 익명화·재식별 (n8n Python 노드 이식본)
  pipeline.ts             # 파이프라인 단계 정의
  csv.ts                  # CSV 파싱 + 샘플 생성
n8n/
  workflow.json           # 본선 실행 경로 (미검증)
  README.md · waitlist.sample.csv
README.md                 # 공개 저장소용
```

---

## 명령

```bash
npm install
npm run dev          # http://localhost:3000
npm run build        # 배포 전 검증
```

환경변수는 `.env.local`. `GEMINI_API_KEY` 없이도 폴백으로 동작한다.

---

## Context

- **직접 사용자**: 코디네이터 2명. 아동은 수혜자이며 사용자가 아니다
- **효용 단위**: 회기(session). 회수된 치료 회기 1건이 가치 1단위
- **규모**: 물리치료사 40 · 작업치료사 40 = 80명, 8종 치료, 관리 아동 약 120명
- **결석**: 전날 안내 10명 내외, 당일 5명 내외. 외래 아동 평균 4건
- **언어치료는 이번 범위에서 제외** (실제로는 언어치료사 10명 존재)
- 산출물은 Apache 2.0으로 공개된다. 기관 내부 자료·개인정보는 공개 대상에서 제외

---

## 프로젝트 소유 커맨드

현재 없음. 필요 시 owner type(Skill/Project/Macro/Alias)을 명시해 등재한다.

---

### 이관 확인 (Changelog 기록용)

- 원본 문서: `보강브릿지_PRD_v0.2.md` (2026-07-30)
- 이관 일자: 2026-07-30
- L1에서 제거 필요한 중복 항목: 없음
- L1 수정 필요 여부: 없음 (프로젝트 고유 내용만 기재)

### 거버넌스 자가검증 (D2)

| 원칙 | 점검 | 결과 |
|---|---|---|
| 1. 단일 소유 | L1 Constraint/Default 텍스트 복제 없음. 포인터만 | ✅ |
| 2. Kind 우선 충돌 | 스택 Default(Supabase/Auth)를 명시적 지정으로 무효화 — D1 상향 우선에 부합 | ✅ |
| 3. 명시적 폐기 | DB·Auth 제외 근거를 표로 기록 | ✅ |
| 4. 입장 심사 | 신규 커맨드 없음 | 해당 없음 |
| 5. 프로젝트 중립 | 이 문서는 L2에만 존재. L1 미수정 | ✅ |
